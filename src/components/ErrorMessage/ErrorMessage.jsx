// import s from './ErrorMessage.module.css';

const ErrorMessage = () => {
    return (
        <div>
            <div>Sorry, we don`t find images</div>
        </div>
    );
};

export default ErrorMessage;